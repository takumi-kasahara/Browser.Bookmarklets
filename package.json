{
  "scripts": {
    "build": "npm run build:minify && npm run build:page",
    "build:minify": "node .tools/build.minify.js",
    "build:page": "node .tools/build.page.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/web": "^0.0.349",
    "@vitest/coverage-v8": "^3.2.7",
    "esbuild": "^0.28.0",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "jsdom": "^25.0.1",
    "vitest": "^3.2.4"
  }
}
